export interface Data {
  currentPage: number;
  totalNumberPages: number;
  pageSize: number;
  bucket: number;
}

export const ReactableHandle = 'prompt-queue';

export const Emoji = {
  Next: '▶',
  Previous: '◀',
};
